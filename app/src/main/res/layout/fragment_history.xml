<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <!-- Header -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:background="@color/surface"
        android:paddingHorizontal="20dp" android:paddingTop="16dp" android:paddingBottom="12dp"
        android:elevation="2dp">

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Repair History" android:textColor="@color/text"
            android:textSize="22sp" android:textStyle="bold" />

        <TextView android:id="@+id/tvRecordCount"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="4 records found" android:textColor="@color/text_dim"
            android:textSize="13sp" android:layout_marginTop="2dp" android:layout_marginBottom="12dp" />

        <!-- Filter tabs -->
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView android:id="@+id/filterAll"
                android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1"
                android:text="All" android:textColor="@color/white" android:textSize="13sp"
                android:textStyle="bold" android:gravity="center"
                android:background="@drawable/bg_btn_primary" android:layout_marginEnd="8dp" />

            <TextView android:id="@+id/filterCompleted"
                android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1"
                android:text="Completed" android:textColor="@color/text_dim" android:textSize="13sp"
                android:gravity="center"
                android:background="@drawable/bg_pill_surface" android:layout_marginEnd="8dp" />

            <TextView android:id="@+id/filterCanceled"
                android:layout_width="0dp" android:layout_height="36dp" android:layout_weight="1"
                android:text="Canceled" android:textColor="@color/text_dim" android:textSize="13sp"
                android:gravity="center"
                android:background="@drawable/bg_pill_surface" />
        </LinearLayout>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvHistory"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="80dp"
        android:clipToPadding="false"
        android:scrollbars="none" />
</LinearLayout>
